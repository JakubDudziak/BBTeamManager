import React from "react";
import Modal from "./Modal.tsx";

export default function CreatePlayerModal() {
    return (
        <Modal isOpen={true} titleContent={"Create Player"} content={
            <>

            </>
        }/>
    )
}